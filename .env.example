POSTGRES_DB=stemlab
POSTGRES_USER=stemlab
POSTGRES_PASSWORD=changeme

# 4-digit PIN for staff access (default: 4321)
ADMIN_PIN=1234

# How long to keep fulfilled/cancelled orders before auto-purge (hours)
ORDER_RETENTION_HOURS=24

PORT=3000
